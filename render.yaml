services:
  - name: zeeniq-editor-api
    type: web
    plan: free
    env: node
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
